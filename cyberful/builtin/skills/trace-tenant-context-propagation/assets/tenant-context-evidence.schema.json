{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cyberful.local/schemas/tenant-context-evidence.v1.json",
  "title": "Cyberful tenant context raw evidence",
  "type": "object",
  "additionalProperties": false,
  "required": ["format", "trace_id", "source_sha256", "limits", "events", "interpretation"],
  "properties": {
    "format": { "const": "cyberful.tenant-context-evidence.v1" },
    "trace_id": { "type": "string" },
    "source_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "limits": { "type": "object" },
    "events": {
      "type": "array",
      "maxItems": 128,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["event_id", "order", "component", "boundary", "evidence_sha256", "context", "changes", "divergences"],
        "properties": {
          "event_id": { "type": "string" },
          "order": { "type": "integer" },
          "component": { "type": "string" },
          "boundary": { "type": "string" },
          "evidence_sha256": { "type": "string" },
          "context": { "type": "object" },
          "changes": { "type": "array", "items": { "type": "string" } },
          "divergences": { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    "interpretation": { "type": "string" }
  }
}
