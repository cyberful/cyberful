interface:
  display_name: "Federated Identity Tester"
  short_description: "Route federation and identity specialists"
  default_prompt: "Use $test-federated-identity to route this federation review across architecture policy propagation workload recovery and linking boundaries."
